---
platform: linux

image_resource:
  type: docker-image
  source: {repository: pcfservicesenablement/services-enablement-ci}

inputs:
- name: backups-final-tag

outputs:
- name: docs-with-tag

run:
  path: backups-final-tag/ci/scripts/push-docs-tag.sh

params: {}
