---
platform: linux

image_resource:
  type: docker-image
  source: {repository: pcfservicesenablement/services-enablement-ci}

inputs:
  - name: service-backup-release
  - name: services-enablement-meta

run:
  path: service-backup-release/ci/scripts/release_tests.sh

params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  AZURE_STORAGE_ACCOUNT:
  AZURE_STORAGE_ACCESS_KEY:
  BOSH_HOST:
  BOSH_CA_CERT_PATH:
  BOSH_CLIENT:
  BOSH_CLIENT_SECRET:
  BOSH_KEY_RELATIVE_PATH:
  S3_BOSH_MANIFEST_RELATIVE_PATH:
  AZURE_BOSH_MANIFEST_RELATIVE_PATH:
  SCP_BOSH_MANIFEST_RELATIVE_PATH:
