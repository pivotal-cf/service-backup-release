---
platform: linux

image_resource:
  type: docker-image
  source: {repository: pcfservicesenablement/services-enablement-ci}

inputs:
- name: service-backup-release

run:
  path: service-backup-release/ci/scripts/unit_integration_tests.sh

params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  AWS_ACCESS_KEY_ID_RESTRICTED:
  AWS_SECRET_ACCESS_KEY_RESTRICTED:
