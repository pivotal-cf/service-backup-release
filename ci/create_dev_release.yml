---
platform: linux

image: docker:///cflondonservices/service-backups-release-ci

inputs:
- name: service-backup-release
- name: services-enablement-meta

run:
  path: service-backup-release/ci/scripts/create_dev_release.sh

params:
  BOSH_URL:
  BOSH_CA_CERT_PATH:
  BOSH_CLIENT:
  BOSH_CLIENT_SECRET:
