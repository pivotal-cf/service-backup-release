---
platform: linux

image_resource:
  type: docker-image
  source: {repository: pcfservicesenablement/services-enablement-ci}

inputs:
- name: backups-final-tag

outputs:
- name: backups-with-tag

run:
  path: backups-final-tag/ci/scripts/push-submodule-tag.sh

params:
  SUBMODULE_PATH: src/github.com/pivotal-cf-experimental/service-backup
  TAG_PATH: backups-with-tag
