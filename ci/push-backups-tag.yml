---
platform: linux

image_resource:
  type: docker-image
  source: {repository: pcfservicesenablement/services-enablement-ci}

inputs:
- name: backups-final-tag

outputs:
- name: backups-with-tag

run:
  path: backups-final-tag/ci/scripts/push-backups-tag.sh

params: {}
