#!/bin/bash -eu

<% if_p('service-backup.source_folder') do %>
  SOURCE_FOLDER='<%= p('service-backup.source_folder') %>'
  mkdir -p "${SOURCE_FOLDER}"
<% end %>

/var/vcap/packages/service-backup/bin/manual-backup /var/vcap/packages/service-backup/config/backup.yml
